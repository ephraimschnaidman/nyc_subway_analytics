version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: mta_postgres
    restart: always
    environment:
      POSTGRES_USER: mta_admin
      POSTGRES_PASSWORD: mta_secure_password
      POSTGRES_DB: mta_transit
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
